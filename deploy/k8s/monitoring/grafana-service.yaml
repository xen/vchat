apiVersion: v1
kind: Service
metadata:
  name: vchat-grafana
  labels:
    app.kubernetes.io/name: vchat
    app.kubernetes.io/component: grafana
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vchat
    app.kubernetes.io/component: grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
