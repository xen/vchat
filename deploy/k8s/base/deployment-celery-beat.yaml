apiVersion: apps/v1
kind: Deployment
metadata:
  name: vchat-celery-beat
  labels:
    app.kubernetes.io/name: vchat
    app.kubernetes.io/component: celery-beat
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: vchat
      app.kubernetes.io/component: celery-beat
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vchat
        app.kubernetes.io/component: celery-beat
    spec:
      serviceAccountName: vchat
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: celery-beat
          image: vchat
          imagePullPolicy: IfNotPresent
          command: ["celery"]
          args: ["-A", "jobs.celery", "beat", "--loglevel=INFO"]
          envFrom:
            - secretRef:
                name: vchat-secret
          env:
            - name: PROMETHEUS_MULTIPROC_DIR
              value: /tmp
          volumeMounts:
            - name: config
              mountPath: /app/local.yaml
              subPath: local.yaml
              readOnly: true
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: config
          configMap:
            name: vchat-config
        - name: tmp
          emptyDir: {}
