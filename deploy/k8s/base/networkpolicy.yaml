apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vchat-web-ingress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: vchat
      app.kubernetes.io/component: web
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - protocol: TCP
          port: 9080
